#include <stdio.h>
#include <string.h>

int main(){
    char* str;

    str = "hello there";
    printf("%c", str[0]);
    printf("%c", str[1]);
    printf("%c", str[2]);

    // printf("enter: ");
    // fgets(str[0],50,stdin);
    // // char* token = strtok(str," ");
    // // while (token != NULL) {
    // //     printf(" % s\n", token);
    // //     token = strtok(NULL, " - ");
    // // }

    // // printf("%s", token[1]);
    // // return 0;
    // printf("%s", str[0]);
}